# Tmux configuration information
There are a few setup steps that needs to happen for this config file to work properly

## Setup
```bash
git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
```
